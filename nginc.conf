worker_processes auto;

events { worker_connections 1024; }

http {
  upstream webcluster {
    server 172.20.0.2:80;
    server 172.20.0.3:80;
  }
  server { listen 80; location / { proxy_pass http://webcluster; } }
}

